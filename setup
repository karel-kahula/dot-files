#!/bin/bash
set -e
bar="======"
files="gitconfig gitignore vimrc git-prompt.sh zshrc tmux.conf"
dotfilesdir=$(pwd)

# copy dot files and directories
cd ~
echo "$bar Creating links for dotfiles $bar"
for file in $files; do
  rm  -rf ~/.$file
  ln -s $dotfilesdir/$file ~/.$file
done
