#!/bin/bash
set -e
bar="======"
files="bash_profile gitconfig gitignore vimrc git-prompt.sh"
dotfilesdir=$(pwd)

# copy dot files and directories
cd ~
echo "$bar Creating links for dotfiles $bar"
for file in $files; do
  rm  -rf ~/.$file
  ln -s $dotfilesdir/$file ~/.$file
done
